extends ../layout
block stylesheets
    link(rel="stylesheet", href="/css/login.min.css")
block content
    .main
        h5 后台管理系统
        form
            .form-control
                label 账号
                input#username(name="username" type="text" placeholder="账户名/手机号")
            .form-control
                label 密码
                input#password(name="password" type="password" placeholder="登录密码")
            .form-control
                label 验证码
                input#smsCode(name="checkCode" type="text" placeholder="手机验证码")
            p.error-tips
        a.submit(href="javascript:;") 登录
block scripts
    script.
        require(['common'], function () {
            require(['page/login']);
        });
